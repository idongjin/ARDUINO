int motor = 3;
int sw = 6;
 
int speed = 127;
 
void setup() {
    pinMode(sw, INPUT_PULLUP);
}
 
void loop() {
    if (digitalRead(sw) == LOW) {
        analogWrite(motor, speed);
    }
 
    else {
        analogWrite(motor, 0);
    }
}
